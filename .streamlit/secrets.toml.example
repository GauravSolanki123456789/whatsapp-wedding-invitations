# Streamlit Cloud → Settings → Secrets
# IMPORTANT: Use Supabase SESSION POOLER (IPv4) — NOT Direct connection (IPv6 only)

# Step 1: Supabase dashboard → Connect → Session pooler → URI
# Step 2: Replace [YOUR-PASSWORD] with your database password
# Host must contain "pooler.supabase.com" — NOT "db.xxxx.supabase.co"

DATABASE_URL = "postgresql://postgres.nilpfwuqhkoyfqbmfpbc:YOUR_PASSWORD@aws-0-ap-south-1.pooler.supabase.com:5432/postgres"

# Only if pooler region is not ap-south-1 (check Supabase Connect dialog):
# SUPABASE_REGION = "us-east-1"

# Optional — WhatsApp Cloud API
# WHATSAPP_ACCESS_TOKEN = "..."
# WHATSAPP_PHONE_NUMBER_ID = "..."
